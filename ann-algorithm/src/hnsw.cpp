#include "anndemo\algorithm\hnsw.hpp"

using namespace ann;

std::vector<int> search_layer(
    const hnsw& hnsw,
    const point& q, 
    const std::vector<int>& ep, int ef, 
    int layer
) {
    return {};
}